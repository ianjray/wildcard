.POSIX:
.SUFFIXES:
.SUFFIXES: .c .o .uto

VERSION    = 1.0.0

CC         = @CC@
CCOV       = gcov
CFLAGS     = @CFLAGS@
CFLAGS_COV = @CFLAGS_COV@
CFLAGS_SAN = @CFLAGS_SAN@
INCLUDEDIR = @PREFIX@/include
LD         = @LD@
LIBDIR     = @PREFIX@/lib
PREFIX     = @PREFIX@

.PHONY: all
all: libwildcard.a

libwildcard.a: wildcard.o
	$(LD) -r $< -o $@

.c.o:
	$(CC) $(CFLAGS) -I. -c $< -o $@

.c.uto:
	$(CC) $(CFLAGS) $(CFLAGS_COV) $(CFLAGS_SAN) -I. -c $< -o $@

wildcard.coverage: wildcard.uto tests/test_wildcard.c
	$(CC) $(CFLAGS) $(CFLAGS_COV) $(CFLAGS_SAN) -I. $^ -o $@
	./$@
	$(CCOV) wildcard.c
	! grep "#####" wildcard.c.gcov |grep -ve "// UNREACHABLE$$"

libwildcard.pc:
	( echo 'Name: libwildcard' ;\
	echo 'Version: $(VERSION)' ;\
	echo 'Description: Wildcard matching in C' ;\
	echo 'prefix=$(PREFIX)' ;\
	echo 'exec_prefix=$${prefix}' ;\
	echo 'includedir=$${prefix}/include' ;\
	echo 'libdir=$${prefix}/lib' ;\
	echo 'Cflags: -I$${includedir}' ;\
	echo 'Libs: -L$${libdir} -lwildcard' ) > $@

.PHONY: test
test: wildcard.coverage

.PHONY: install
install: wildcard.h libwildcard.a libwildcard.pc
	mkdir -p $(DESTDIR)$(INCLUDEDIR)/libwildcard
	mkdir -p $(DESTDIR)$(LIBDIR)/pkgconfig
	install -m644 wildcard.h $(DESTDIR)$(INCLUDEDIR)/libwildcard/wildcard.h
	install -m644 libwildcard.a $(DESTDIR)$(LIBDIR)/libwildcard.a
	install -m644 libwildcard.pc $(DESTDIR)$(LIBDIR)/pkgconfig/libwildcard.pc

.PHONY: uninstall
uninstall:
	rm -f $(DESTDIR)$(INCLUDEDIR)/libwildcard/wildcard.h
	rm -f $(DESTDIR)$(LIBDIR)/libwildcard.a
	rm -f $(DESTDIR)$(LIBDIR)/pkgconfig/libwildcard.pc

.PHONY: clean
clean:
	rm -f *.o **/*.o *.uto **/*.uto *.gc?? **/*.gc?? *.coverage
	rm -f libwildcard.a libwildcard.pc

.PHONY: distclean
distclean: clean
	rm -f Makefile config.status
